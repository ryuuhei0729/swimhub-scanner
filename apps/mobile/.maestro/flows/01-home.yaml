# ホーム（スキャン開始画面）
# カメラはシミュレータで使えないため、スキャンは実行せず idle 状態の見た目だけ撮る。
# 必要な env: APP_ID, LOCALE, REGION, APP_EMAIL, APP_PASSWORD, OUTPUT_DIR, DEVICE_TAG
appId: ${APP_ID}
---
- runFlow: ../subflows/login.yaml
- waitForAnimationToEnd:
    timeout: 5000
- takeScreenshot: ${OUTPUT_DIR}/${DEVICE_TAG}_01_home
